\ProvidesPackage{mypackages}
%=================================================================
%		packages that need to be loaded first
%=================================================================
% \usepackage{etex} something old I guess
% \usepackage{calc}  % for setting counters, it seems
% \usepackage{ifthen}
% \usepackage{ifpdf}

% \newboolean{color}\setboolean{color}{true}		              % switch between color and black/white
% \newboolean{show-comments}\setboolean{show-comments}{true}    % show conditional comments (\ccom) on/off (e.g. for drafts)

%=-=-=-=-=-=-=-=-

\usepackage{titling}
\predate{}
\postdate{}
\posttitle{\par\end{center}}
\usepackage[svgnames]{xcolor}
\usepackage{tabularx} % for tables with auto line-break
\usepackage{enumitem} % for control of enumerate labels http://ctan.org/pkg/enumitem

\usepackage{tocloft}% http://ctan.org/pkg/tocloft
\setlength{\cftsubsecnumwidth}{4em}% Set length of number width in ToC for \subsection
\setlength{\cftsecnumwidth}{4em}% Set length of number width in ToC for \section
\makeatother

%=================================================================
%			bibliography
%=================================================================
% \usepackage{natbib}
% \bibpunct[:]{(}{)}{;}{a}{}{,}	% this command defines:
%                              	% pages delimiter, style for opening & closing bracket
%                                 % reference delimiter, counter, space, and year delimiter
% \usepackage[sectionbib]{chapterbib}

%=-=-=-=-=-=-=-=-

\usepackage[longnamesfirst]{natbib}
\bibliographystyle{apalike}
\setcitestyle{authoryear,round,semicolon,
aysep={},yysep={,},notesep={:}}
\bibpunct[:]{(}{)}{,}{a}{}{;}

%=================================================================
%			fonts
%=================================================================
\usepackage[utf8]{inputenc}	% does not work with ð and þ; use \eth and \thorn instead
\usepackage[T1]{fontenc}	% scalable EC fonts
% \usepackage[bitstream-charter]{mathdesign}% math fonts matched for charter, garamond, times, utopia
% \DeclareMathAlphabet{\mathsc}{T1}{mdbch}{m}{fsc}
\usepackage{skull}  % for a dangerous skull - me likey
% \usepackage{bbding} % for checkmarks and failures

% % use microtype with pdflatex for optimized kerning, protrusion etc.
% % see the microtype manual for additional options
% \ifpdf
%     \usepackage[kerning=true, draft=false]{microtype}
% \fi

%=-=-=-=-=-=-=-=-

% \usepackage{charter} % for Charter font, takes more space yet looks more compact
% \usepackage{newtxtext,newtxmath} % fornewtx that is very similar to times new roman
\usepackage{mathptmx} % another time new roman
% \usepackage{kpfonts} % for kp fonts
% \usepackage{fontspec} % for lualatex and the other builder
% \usepackage[normalem]{ulem} % for text decoration
\usepackage{fontawesome} % take a guess, what is it for
% \usepackage{lmodern}% http://ctan.org/pkg/lm
% \usepackage{anyfontsize} % to remove font shape warning
% \usepackage{hyperref}

%=================================================================
%			glosses
%=================================================================
% \makeatletter
% \def\new@fontshape{}
% \makeatother
\usepackage{gb4e}		% gb4e with indices outside mathmode deactivated (looking for replacement)
\noautomath
% \let\eachwordone=\it
% \makeatletter
% \@addtoreset{exx}{part}  % this resets example numbers for \part
% \makeatother

% adjust vertical spacing for gb4e here!


%=-=-=-=-=-=-=-=-

% \makeatletter
% \apptocmd{\@exe}{\setstretch{0.7}}{}{}
% \makeatother

% \setlength{\parindent}{1em}
% \setlength{\parskip}{0pt}
% \setlength{\parsep}{0pt}
% \setlength{\headsep}{0pt}
% \setlength{\topskip}{0pt}
% \setlength{\topmargin}{0pt}
% \setlength{\topsep}{0pt}
% \setlength{\partopsep}{0pt}
% \setlength{\headheight}{0pt}
% \setlength{\headsep}{0pt}
\pagestyle{plain} % header/foot style plain for page number, empty for, well, empty



%=================================================================
%			graphics
%=================================================================
% \ifpdf				% load color with different settings for latex and pdflatex
%     \ifthenelse{\boolean{color}}	% switch between color and black/white; set in myifthensettings.sty
%     {\usepackage[table,usenames,dvipsnames,svgnames,x11names]{xcolor}}
%     {\usepackage[table,usenames,dvipsnames,monochrome,svgnames,x11names]{xcolor}}
% \else
%     \ifthenelse{\boolean{color}}	% switch between color and black/white; set in myifthensettings.sty
%     {\usepackage[table,usenames,dvips]{color}}
%     {\usepackage[table,usenames,dvips,monochrome]{color}}
% \fi

% \ifpdf				% load graphicx with different settings for latex and pdflatex
% 	\usepackage[pdftex]{graphicx}
% 	\DeclareGraphicsExtensions{.pdf,.png,.jpg,.mps}
% \else
% 	\usepackage[dvips]{graphicx}
%     \DeclareGraphicsExtensions{.eps}
% \fi
% \definecolor{theblue}{rgb}{0.02.,0.04,0.48}

% \usepackage{framed} % for nice paragraph styles
% \usepackage{tikz}
% \usepackage{tikz-qtree}
% \usetikzlibrary{arrows,automata,backgrounds,calc,chains,decorations.pathmorphing,decorations.text,external,fadings,fit,matrix,scopes,shadows,shapes,through,trees}
% \tikzexternalize
% \tikzsetexternalprefix{build/tikz/}

% \usepackage{marginnote}

%=-=-=-=-=-=-=-=-

\usepackage{tikz} % for linguistic trees
\usepackage{tikz-dependency} % for dependency trees
\usepackage{tikz-qtree} % for linguistic trees
\usepackage{forest} % for linguistic trees, depends on the above packages? % watch out for empty lines!
% baseline, for tree={parent anchor=south, child anchor=north, align=center, l sep+=-.5em, s sep+=0em}

\useforestlibrary{linguistics}
\forestapplylibrarydefaults{linguistics} % adding theses two gives you roof
% \usetikzlibrary{positioning}

% define move style
\usetikzlibrary{arrows.meta}
\tikzset{move/.style = {-{Latex[length=.5em]},dashed,blue}}
\tikzset{rightward/.style = {dotted}}
\tikzset{annotation/.style = {font=\footnotesize}}
\tikzset{index/.style = {annotation, anchor=south east}}
\tikzset{outdex/.style = {annotation, anchor=north west}}
\tikzset{boxed/.style = {draw}}
\tikzset{empty/.style = {Pink}}
\tikzset{non-final/.style = {opacity=70}}

% define flowchart style
\usetikzlibrary{shapes.geometric, arrows}
\tikzstyle{startstop} = [rectangle, rounded corners, minimum height=1cm, text width=4.5em, text centered, draw=black]
\tikzstyle{io} = [rectangle, rounded corners, minimum height=1cm, text width=4.5em, text centered, draw=black]
\tikzstyle{process} = [rectangle, minimum height=1cm, text width=4.5em, text centered, draw=black]
%=================================================================
%			language
%=================================================================
% \usepackage[english]{babel}	% if you are using several languages, the last one is the default
% \selectlanguage{english}


%=================================================================
%			math typesetting
%=================================================================
% \RequirePackage{amsmath}	% AMS special settings for mathematics
% \RequirePackage[amsmath,hyperref,thmmarks]{ntheorem}
% \RequirePackage{cancel}
% \RequirePackage[algochapter,linesnumbered,vlined,ruled]{algorithm2e}
% \RequirePackage{bussproofs}


%=-=-=-=-=-=-=-=-

\usepackage{amsmath}    % for semantic representation
\usepackage{amssymb} % for a decent-looking symbol for the empty set
\usepackage{stmaryrd}   % for semantic representation [[ ]]
\usepackage[makeroom]{cancel}

%=================================================================
%			misc
%=================================================================
% \usepackage{minitoc}
% \usepackage{array}
% \newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
% \usepackage{booktabs}
% \usepackage{mdwlist}
% \usepackage[titles]{tocloft}
% \usepackage{longtable}
% \usepackage{multirow}
% \usepackage{rotating}
% \usepackage{setspace}		% for single-spacing in floats and footnotes
% \usepackage{subcaption}
% \usepackage{xspace}

%=-=-=-=-=-=-=-=-

\usepackage{setspace}
\usepackage{multicol} % for multiple columns
\setlength{\multicolsep}{-10 pt} %change space before and after columns
\setlength{\columnsep}{0.1cm} %for col separation

\usepackage{wrapfig}
\usepackage{titling}
\predate{}
\postdate{}
\posttitle{\par\end{center}}

\usepackage{titlesec} % for chpt, title styles
\usepackage{setspace}
\usepackage{csquotes} % for quotations

% use the following to use \part{} to reset section and example number. There must be a better way, though.
\makeatletter
\@addtoreset{section}{part}
\makeatother
\titleclass{\part}{straight}
\titleformat{\part}[display]
{\normalfont\bfseries}{}{0pt}{} % for the style of the part title

% \titleformat{\chapter}[display] % shape
% {\bfseries\Large\itshape} % format
% {Story No. \ \thechapter} % label
% {0.5ex} % sep
% {
%     \rule{\textwidth}{1pt}
%     \vspace{1ex}
%     \centering
% } % before-code
% [
% \vspace{-0.5ex}%
% \rule{\textwidth}{0.3pt}
% ] % after-code


\titlespacing{\section}{0pc}{1.5ex plus .1ex minus .2ex}{1pc}
\titleformat{\section}
{\bfseries}
{\thesection.}{0.9em}{}
\titlespacing{\subsection}{0pc}{1.5ex plus .1ex minus .2ex}{.5pc}
\titleformat{\subsection}
{\bfseries}%{\normalfont}
{\thesubsection.}{0.9em}{}

\titlespacing{\paragraph}{%
  0pt}{%              left margin
  0\baselineskip}{% space before (vertical)
  1em}%               space after (horizontal)

\setlength{\droptitle}{-5cm} % moving title up

%=================================================================
%			programming
%=================================================================
% \usepackage[chapter]{minted}


%=================================================================
%			references
%=================================================================
% \usepackage{varioref}


%=================================================================
%		packages that need to be loaded last
%=================================================================
\usepackage[top=2cm, bottom=2.2cm, left=2cm, right=2cm]{geometry}
\usepackage{fancyhdr}

% \makeatletter
% \def\cleardoublepage{\clearpage\if@twoside \ifodd\c@page\else
%     \hbox{}
%     \vspace*{\fill}
%     % \begin{center}
%     %     This page intentionally left blank
%     % \end{center}
%     % \vspace{\fill}
%     \thispagestyle{empty}
%     \newpage
%     \if@twocolumn\hbox{}\newpage\fi\fi\fi}
% \makeatother

% % increase the header height to fit in all the text on the front page;
% \addtolength{\headheight}{\baselineskip}

% % set some basic parameters of the header layout
% \newcommand{\HeaderFontSize}{\footnotesize}
% \newcommand{\WPLheadrulewidth}{.6pt}

% % headers don't have a separating line on a page consisting only of floats,
% % else they use the separator defined by \@WPLheadrulewidth above;
% % foots never have a separating line
% \renewcommand{\headrulewidth}{\iffloatpage{0pt}{\WPLheadrulewidth}}
% \renewcommand{\footrulewidth}{0pt}

% % define the page headers for everything but the front page;
% % first we remove the standard headers and footers
% \fancyhf{}
% \fancyheadoffset[RO,LE]{\marginparsep+\marginparwidth}
% \fancyhead[C]{\HeaderFontSize \lastname\ - \coursename, \semester}
% \fancyhead[RO,LE]{\bfseries \thepage}

% \fancypagestyle{plain}{
% 	\fancyhf{}
%     \renewcommand{\headrulewidth}{0pt}
% }

% \ifpdf				% optionally load hyperref
% 	\usepackage[pdftex,%
% 			backref=false,%			add link to section of citation to each item in bibliography
% 			pagebackref=false,%		add link to page of citation to each item in bibliography
% 			unicode=true,%			non-Latin characters in Acrobat’s bookmarks
% 			breaklinks=true,%		allow link texts to break across lines
% 			pageanchor=true,%		if set to false, the TOC will have no links
% 			colorlinks=true,%		false: boxed links; true: colored links
% 			linkcolor=theblue,%		color of internal links
% 			citecolor=theblue,%		color of links to bibliography
% 			filecolor=theblue,%		color of file links
% 			urlcolor=theblue,%		color of external links
% 			pdfauthor={\theauthor},%	as specified by you in frame.tex
% 			pdftitle={Local and Transderivational Constraints in Syntax and Semantics},%		as specified by you in frame.tex
% 			pdfkeywords={\thekeywords}%	as specified by you in frame.tex
% 		]{hyperref}%
% %
% % since hyperref links to captions, it links to the bottom of tables and figures;
% % in order to fix this problem, the hypcap package redefines table, table*, figure, and figure*
% % such that their caption is at the top;
% % CAUTION: This means it will clash with any package that redefines \caption or \@caption
% \usepackage[all]{hypcap}
% \fi
